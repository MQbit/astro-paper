---
const { title, description, slug } = Astro.props;
---

<div class="card bg-white p-6 rounded-lg shadow-md">
  <h2 class="text-xl font-bold">{title}</h2>
  <p class="text-gray-700">{description}</p>
  <a href={`/AgentsHunt/${slug}`}>Learn More →</a>
</div>
